"use client";

import { useState } from "react";
import { MOCK_LABS } from "@/lib/mock-labs";

export default function LabFinder() {
  const [query, setQuery] = useState("");
  const [state, setState] = useState("All locations");
  const [scope, setScope] = useState("All scopes");
  const states = ["All locations", ...Array.from(new Set(MOCK_LABS.map((lab) => lab.state)))];
  const scopes = ["All scopes", ...Array.from(new Set(MOCK_LABS.flatMap((lab) => lab.scopes)))].sort();
  const needle = query.trim().toLowerCase();
  const results = MOCK_LABS.filter((lab) => {
    const searchable = [lab.name, lab.address, lab.city, lab.state, ...lab.scopes].join(" ").toLowerCase();
    return (!needle || searchable.includes(needle)) && (state === "All locations" || lab.state === state) && (scope === "All scopes" || lab.scopes.includes(scope));
  });

  return <div className="space-y-5"><section className="rounded-xl border border-[var(--color-border)] bg-white p-5 shadow-sm sm:p-6"><div className="flex flex-col gap-2 sm:flex-row sm:items-start sm:justify-between"><div><p className="text-xs font-semibold uppercase tracking-wider text-[var(--color-navy)]">BIS directory</p><h2 className="mt-1 text-xl font-semibold text-gray-900">Find a testing laboratory</h2><p className="mt-1 max-w-xl text-sm text-gray-600">Search by product scope, city, or state to find a local testing facility.</p></div><span className="text-sm text-gray-500"><strong className="text-gray-900">{results.length}</strong> matches</span></div><div className="mt-5 grid gap-3 md:grid-cols-[1fr_190px_190px]"><label className="relative"><span className="sr-only">Search laboratories</span><svg className="pointer-events-none absolute left-3 top-3 h-5 w-5 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="m21 21-4.35-4.35m2.35-5.65a8 8 0 1 1-16 0 8 8 0 0 1 16 0Z" /></svg><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search cement, Gujarat, Vadodara..." className="h-11 w-full rounded-lg border border-gray-300 pl-10 pr-3 text-sm shadow-sm placeholder:text-gray-400 focus:border-[var(--color-navy)] focus:outline-none focus:ring-2 focus:ring-[var(--color-navy)]/20" /></label><label><span className="sr-only">Filter by location</span><select value={state} onChange={(event) => setState(event.target.value)} className="h-11 w-full rounded-lg border border-gray-300 bg-white px-3 text-sm text-gray-700 shadow-sm focus:border-[var(--color-navy)] focus:outline-none focus:ring-2 focus:ring-[var(--color-navy)]/20">{states.map((item) => <option key={item}>{item}</option>)}</select></label><label><span className="sr-only">Filter by test scope</span><select value={scope} onChange={(event) => setScope(event.target.value)} className="h-11 w-full rounded-lg border border-gray-300 bg-white px-3 text-sm text-gray-700 shadow-sm focus:border-[var(--color-navy)] focus:outline-none focus:ring-2 focus:ring-[var(--color-navy)]/20">{scopes.map((item) => <option key={item}>{item}</option>)}</select></label></div></section><section className="grid gap-4 md:grid-cols-2" aria-live="polite">{results.map((lab) => <article key={lab.id} className="rounded-xl border border-[var(--color-border)] bg-white p-5 shadow-sm"><div className="flex items-start justify-between gap-3"><div><h3 className="font-semibold text-gray-900">{lab.name}</h3><p className="mt-1 text-sm text-gray-600">{lab.address}, {lab.state}</p></div><span className="shrink-0 rounded-full bg-green-50 px-2 py-1 text-[11px] font-semibold text-green-700">{lab.status}</span></div><div className="mt-4"><p className="text-xs font-semibold uppercase tracking-wide text-gray-500">Recognised scopes</p><div className="mt-2 flex flex-wrap gap-2">{lab.scopes.map((item) => <span key={item} className="rounded-md bg-[var(--color-navy-lighter)] px-2 py-1 text-xs font-medium text-[var(--color-navy)]">{item}</span>)}</div></div><div className="mt-4 space-y-1 border-t border-gray-100 pt-3 text-sm text-gray-600"><p><span className="font-medium text-gray-800">Phone:</span> {lab.phone}</p><p className="break-all"><span className="font-medium text-gray-800">Email:</span> {lab.email}</p></div></article>)}{!results.length && <div className="col-span-full rounded-xl border border-dashed border-gray-300 bg-white px-6 py-12 text-center"><h3 className="font-semibold text-gray-900">No matching labs</h3><p className="mt-1 text-sm text-gray-600">Try another location or product scope.</p></div>}</section></div>;
}