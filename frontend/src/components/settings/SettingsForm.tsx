"use client";

import { useState } from "react";

type Language = "en" | "hi";

export default function SettingsForm() {
  const [language, setLanguage] = useState<Language>("en");
  const [expiryReminders, setExpiryReminders] = useState(true);
  const [productUpdates, setProductUpdates] = useState(true);
  const [password, setPassword] = useState("");
  const [passwordConfirmation, setPasswordConfirmation] = useState("");
  const [passwordSubmitted, setPasswordSubmitted] = useState(false);
  const [saved, setSaved] = useState(false);
  const [deleteDialogOpen, setDeleteDialogOpen] = useState(false);

  function markSaved() {
    setSaved(true);
  }

  function changePassword(event: React.FormEvent<HTMLFormElement>) {
    event.preventDefault();
    setPasswordSubmitted(true);
    if (!password || password !== passwordConfirmation) return;
    setPassword("");
    setPasswordConfirmation("");
    setPasswordSubmitted(false);
    markSaved();
  }

  const passwordError = passwordSubmitted && !password ? "Enter a new password." : passwordSubmitted && password !== passwordConfirmation ? "Passwords do not match." : "";

  return <main className="flex-1 overflow-y-auto bg-gray-50" id="main-content"><div className="mx-auto w-full max-w-3xl px-4 py-8 sm:px-6 lg:px-8"><div className="mb-7"><p className="text-xs font-semibold uppercase tracking-wider text-[var(--color-navy)]">Account</p><h1 className="mt-1 text-2xl font-bold text-gray-900">Settings</h1><p className="mt-2 text-sm text-gray-600">Control your BIS Sahayak preferences for this session.</p></div><div className="space-y-5"><section className="rounded-xl border border-[var(--color-border)] bg-white p-6 shadow-sm sm:p-8"><div className="mb-6"><h2 className="text-lg font-semibold text-gray-900">Preferences</h2><p className="mt-1 text-sm text-gray-600">Choose how BIS Sahayak communicates with you.</p></div><div><label htmlFor="settings-language" className="text-sm font-semibold text-gray-900">Language</label><select id="settings-language" value={language} onChange={(event) => { setLanguage(event.target.value as Language); markSaved(); }} className="mt-2 h-11 w-full rounded-lg border border-gray-300 bg-white px-3 text-sm text-gray-900 shadow-sm focus:border-[var(--color-navy)] focus:outline-none focus:ring-2 focus:ring-[var(--color-navy)]/20"><option value="en">English</option><option value="hi">हिंदी</option></select><p className="mt-1 text-xs text-gray-500">Language preference is currently local to this interface.</p></div></section><section className="rounded-xl border border-[var(--color-border)] bg-white p-6 shadow-sm sm:p-8"><div className="mb-6"><h2 className="text-lg font-semibold text-gray-900">Notifications</h2><p className="mt-1 text-sm text-gray-600">Choose which updates you would like to see in the future.</p></div><div className="divide-y divide-gray-100"><label className="flex cursor-pointer items-center justify-between gap-4 py-4 first:pt-0"><span><span className="block text-sm font-semibold text-gray-900">Document expiry reminders</span><span className="mt-1 block text-xs text-gray-500">Get a reminder when saved documents or certifications need attention.</span></span><input type="checkbox" checked={expiryReminders} onChange={(event) => { setExpiryReminders(event.target.checked); markSaved(); }} className="h-5 w-5 shrink-0 accent-[var(--color-navy)]" /></label><label className="flex cursor-pointer items-center justify-between gap-4 py-4 last:pb-0"><span><span className="block text-sm font-semibold text-gray-900">Chat/product updates</span><span className="mt-1 block text-xs text-gray-500">Hear about new assistant capabilities and BIS product guidance.</span></span><input type="checkbox" checked={productUpdates} onChange={(event) => { setProductUpdates(event.target.checked); markSaved(); }} className="h-5 w-5 shrink-0 accent-[var(--color-navy)]" /></label></div></section><section className="rounded-xl border border-[var(--color-border)] bg-white p-6 shadow-sm sm:p-8"><div className="mb-6"><h2 className="text-lg font-semibold text-gray-900">Account</h2><p className="mt-1 text-sm text-gray-600">These controls are mock-only in this prototype.</p></div><form onSubmit={changePassword} className="space-y-4"><div><label htmlFor="new-password" className="text-sm font-semibold text-gray-900">Change password</label><input id="new-password" type="password" value={password} onChange={(event) => setPassword(event.target.value)} placeholder="New password" className="mt-2 h-11 w-full rounded-lg border border-gray-300 px-3 text-sm text-gray-900 shadow-sm placeholder:text-gray-400 focus:border-[var(--color-navy)] focus:outline-none focus:ring-2 focus:ring-[var(--color-navy)]/20" /></div><div><label htmlFor="confirm-password" className="sr-only">Confirm new password</label><input id="confirm-password" type="password" value={passwordConfirmation} onChange={(event) => setPasswordConfirmation(event.target.value)} placeholder="Confirm new password" className="h-11 w-full rounded-lg border border-gray-300 px-3 text-sm text-gray-900 shadow-sm placeholder:text-gray-400 focus:border-[var(--color-navy)] focus:outline-none focus:ring-2 focus:ring-[var(--color-navy)]/20" />{passwordError && <p className="mt-1 text-xs text-red-700">{passwordError}</p>}</div><div className="flex flex-col gap-3 pt-1 sm:flex-row sm:items-center"><button type="submit" className="h-10 rounded-lg bg-[var(--color-navy)] px-4 text-sm font-semibold text-white hover:bg-[var(--color-navy-light)] focus:outline-none focus:ring-2 focus:ring-[var(--color-navy)] focus:ring-offset-2">Update password</button>{saved && <p className="text-sm font-medium text-green-700" role="status" aria-live="polite">Saved.</p>}</div></form><div className="mt-7 flex flex-col gap-3 border-t border-gray-100 pt-6 sm:flex-row sm:items-center sm:justify-between"><div><h3 className="text-sm font-semibold text-gray-900">Delete Account</h3><p className="mt-1 text-xs text-gray-500">This prototype will not delete anything.</p></div><button type="button" onClick={() => setDeleteDialogOpen(true)} className="h-10 rounded-lg border border-red-200 px-4 text-sm font-semibold text-red-700 hover:bg-red-50 focus:outline-none focus:ring-2 focus:ring-red-500 focus:ring-offset-2">Delete Account</button></div></section></div></div>{deleteDialogOpen && <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/30 px-4" role="presentation"><div role="dialog" aria-modal="true" aria-labelledby="delete-account-title" className="w-full max-w-sm rounded-xl border border-gray-200 bg-white p-6 shadow-xl"><h2 id="delete-account-title" className="text-lg font-semibold text-gray-900">Delete this account?</h2><p className="mt-2 text-sm leading-6 text-gray-600">This is a mock action. No account or data will be deleted.</p><div className="mt-6 flex justify-end gap-3"><button type="button" onClick={() => setDeleteDialogOpen(false)} className="rounded-lg border border-gray-300 px-4 py-2 text-sm font-semibold text-gray-700 hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-[var(--color-navy)]">Cancel</button><button type="button" onClick={() => setDeleteDialogOpen(false)} className="rounded-lg bg-red-700 px-4 py-2 text-sm font-semibold text-white hover:bg-red-800 focus:outline-none focus:ring-2 focus:ring-red-500">Close</button></div></div></div>}</main>;
}
